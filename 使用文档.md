# 微信公众号排版编辑器 — 使用文档

## 简介

本编辑器专为「谓无名」微信公众号排版设计，基于 Markdown 语法扩展了多种自定义语法，可一键生成符合公众号风格的富文本排版。左侧输入 Markdown，右侧实时预览手机端效果，点击「复制排版结果」即可粘贴到公众号编辑器。

---

## 基本操作

| 操作 | 方式 |
|------|------|
| 输入内容 | 左侧编辑区直接输入 Markdown + 自定义语法 |
| 实时预览 | 输入后右侧自动刷新预览 |
| 复制排版 | 点击右上角「复制排版结果」按钮，或按 `Ctrl+Enter` |
| 撤销/重做 | 工具栏撤销/重做按钮，或 `Ctrl+Z` / `Ctrl+Y` |
| 载入示例 | 点击「载入示例」按钮查看完整示例 |
| 语法帮助 | 点击「语法说明」按钮展开/收起语法参考 |

---

## 主题配色

编辑器支持两套可自定义的颜色：

### 主题色

影响小标题、编者按标签、注释编号、关注引导文字等元素的颜色。预设 5 种：

- 谓无名蓝 `#3daad6`（默认）
- 典雅紫 `#7c5cbf`
- 古铜棕 `#a17a7a`
- 翠绿 `#2d9a4e`
- 砖红 `#c45c3e`

也可点击最右侧色块自定义任意颜色。

### 书籍色

影响书籍信息区的书名、元数据等文字颜色。预设 5 种：

- 默认灰 `#888888`（默认）
- 青灰 `#6b7b8d`
- 棕灰 `#7a6b6b`
- 绿灰 `#5a7a5a`
- 深灰 `#555555`

---

## 标准 Markdown 语法

编辑器支持标准 Markdown 语法，以下是常用项：

| 语法 | 效果 | 说明 |
|------|------|------|
| `# 标题` | 一级标题 | 居中，17px，黑色 |
| `**粗体**` | **粗体** | 加粗 |
| `*斜体*` | *斜体* | 斜体 |
| `> 引用` | 引用块 | 左侧灰色竖线，14px 灰色宋体 |
| `-- 作者` | 引文署名 | 写在引用块内单独一行，自动右对齐 |
| `<u>文字</u>` | 下划线 | Markdown 没有下划线语法，直接写标签 |
| `---` | 分割线 | 自动渲染为点线圆点分割线 |
| `![alt](url)` | 图片 | 居中显示 |
| `[链接](url)` | 超链接 | 主题蓝色 |

> **注意：** 编辑器会自动将英文直引号 `"` 转换为中文引号 `""`，无需手动处理。

---

## 自定义语法

以下为本编辑器专有的扩展语法，用于生成微信公众号特有排版。

### 0. 排版微调（字体 / 字号 / 行距 / 字距 / 缩进 / 间距 / 对齐）

有两层，逐处的优先级高于全局。

#### 全局：顶部「排版微调」面板

按 **正文 / 引文 / 图注** 三组分别设置，改哪一项就只覆盖哪一项，**留空＝保持默认**。
什么都不动时，渲染结果与没有这个功能时逐像素一致。

| 组 | 可调项 |
|---|---|
| 正文 | 字体、字号、行间距、字间距、首行缩进、段间距（空行倍数）、对齐 |
| 引文 | 字体、字号、行间距、字间距、首行缩进、上下间距（px） |
| 图注 | 字体、字号、行间距、字间距、对齐 |

「图注」指书籍信息 / `[universal:]` / `[origin:]` / 延伸阅读里那几行灰色小字。

设置会同时作用于预览区和两条复制路径（「复制排版结果」按钮、手动选中 Ctrl+C）。
落点都在最外层容器上，所以粘进公众号后**仍然可以再用微信自己的行间距/字间距工具微调**。

#### 逐处：`[style:...]`

单独占一行，作用于**紧随其后的那一块**：

```
[style:lh=2.2|ls=1px|size=15]
> 这段引文单独用更松的行距。
> --作者
```

| 键 | 含义 | 省略单位时 |
|---|---|---|
| `lh` | 行间距 | 无单位倍数 |
| `ls` | 字间距 | px |
| `size` | 字号 | px |
| `font` | 字体 | 可写 宋体 / 黑体 / 楷体 / 仿宋 / 无衬线，或直接写完整字体栈 |
| `indent` | 首行缩进 | em（1 = 一个字） |
| `align` | 对齐 | justify / left / center / right |
| `mt` / `mb` | 上 / 下间距 | px |
| `color` | 文字颜色 | 直接写 `#cc0000` 这类色值 |

只写需要改的键，用 `|` 分隔。带单位也行（`ls=0.08em`），会原样使用。

### 1. 引文署名

引用块里想给引文标出处时，**另起一行以 `--` 开头**即可，会自动右对齐：

```
> 对“通史”编撰的政治需求，在任何时代都是犹如锦上添花般的存在。
> --鲁迅《野草》
```

- `--`、`---`、`—`、`——` 都能触发，渲染时统一成中文破折号 `——`
- 署名行必须**单独成行**；行首打空格没有用（HTML 会把连续空格折叠成一个）
- 署名行仍在同一个引用框内，不会另起一个引用块

### 2. 编者按框

用 `>>>` 和 `<<<` 包裹内容，生成四边细线框，带蓝色「编者按」标签和灰色正文。

```
>>> 这是编者按的第一段内容。

这是编者按的第二段内容。
<<<
```

- 编者按内空行会被忽略，每行渲染为独立段落
- 编者按框底部有装饰圆点

### 3. 书籍信息

#### 中文书籍 `[book:]`

```
[book:封面URL|书名|作者|出版社|年份]
```

- 封面居中，188px 宽，带阴影
- 封面上方有 ▲ 标记
- 书名自动添加《》
- 书名和元数据为灰色 12px 宋体
- 封面 URL 可留空

```
[book:https://example.com/cover.jpg|浮世通鉴|张三 译|北京大学出版社|2025]
[book:|无封面书名|作者|出版社|2025]
```

#### 英文书籍 `[enbook:]`

```
[enbook:封面URL|English Title|Author|Publisher|Year]
```

- 与中文书籍格式相同，但书名为斜体且不加书名号
- 封面 URL 可留空

#### 日语书籍 `[jpbook:]`

```
[jpbook:封面URL|日本語タイトル|著者|出版社|年]
```

- 书名自动添加『』书名号
- 封面宽 189px（比中文/英文多 1px）
- 封面 URL 可留空

#### 书籍封面图 `![book](url)`

```
![book](https://example.com/cover.jpg)
```

- 单独展示书籍封面，居中带阴影，188px 宽
- URL 可留空

### 4. 通用图片标识 `[universal:]`

适用于人物介绍、物品展示等场景，居中显示照片 + ▲ + 多行文字。

```
[universal:照片URL|第一行文字|第二行文字|第三行文字|...]
```

- 用 `|` 分隔，至少 3 个字段（照片URL + 至少 2 行文字）
- 第一行文字加粗显示
- 后续行灰色小字
- 人物生卒年用波浪号 `~`，**波浪号后空一格再写卒年**：`舒群（1913~ 89）`；在世人物写 `姓名（生年~）`，波浪号后不留空格

```
[universal:https://example.com/photo.jpg|舒群（1913~ 89）|作家，代表作《没有祖国的孩子》]
```

### 5. 标题与分割线

#### 章节大标题（分割线上方）

```
### 标题 / 作者
```

- 居中显示，蓝灰色 `#7d90a8`
- 标题和作者之间用 ` / ` 分隔（斜杠前后各一空格）
- 用于文章章节开头

#### 小标题（分割线下方）

```
## 标题
```

- 居中显示，主题蓝色加粗，16px 宋体

#### 点线分割线

```
---[dot]
```

- 渲染为 `···●···` 样式的点线圆点分割线
- 普通 `---` 也会自动渲染为此样式

### 6. 脚注编号

```
^①
```

- 在正文中插入蓝色上标脚注编号
- 支持带圈数字 ①~㉚

### 7. 外文注释

半角或全角括号内的外文内容会自动渲染为灰色 12px 小字：

```
酷日本（Cool Japan）
博歌乐（Vocaloid）
```

- 括号内的英文自动变灰
- 不影响中文括号内的中文内容

### 8. 注释区（可滚动）

```
---[notes]

^[① 脚注文本内容]

^[② 另一条脚注文本]

---[/notes]
```

- 注释区标题为黑色加粗 14px「注释」二字
- 标题下方有细线分隔
- 脚注内容在 240px 高的滚动区内显示
- 每条脚注格式：`^[编号 内容]`，编号为蓝色，文本为灰色 12px

### 9. 尾注区

```
---[note]

- ・ 第一条尾注
- ・ 第二条尾注
- ・ 第三条尾注

---[/note]
```

- 尾注为右对齐灰色 12px 文字
- 每条以 `・` 开头
- 也可不加 `・`，编辑器会自动添加

### 10. 目录区（可滚动）

```
---[toc]

序 引言 / 001

**第一章 标题 / 029**
1 小节名 / 030
2 小节名 / 042

**第二章 标题 / 063**

---[/toc]
```

- 自动生成居中蓝色「目 录」标题
- 标题下方有分割线
- 目录内容在 240px 滚动区内显示
- 支持使用 `**粗体**` 标记章节标题

### 11. 作者/译者简介区

```
---[bio-title:作者简介]

[bio-img:https://example.com/photo.jpg]

[bio:简介文字内容，可以写多行。]

---[/bio]
```

- 标题为蓝色加粗 16px
- 标题下方有分割线
- 照片居中，156px 宽
- 简介文字 14px 灰色
- 可多次使用（如分别展示作者简介和译者简介）

### 12. 延伸阅读区

```
---[reading-title:延伸阅读]

[reading-book:书名|编者|译者|封面URL]

[reading-enbook:English Title|Editor|Publisher|封面URL]

[reading-jpbook:日本語タイトル|編者|出版社|封面URL]

---[/reading]
```

- 标题为蓝色加粗 16px
- 中文书名自动添加《》，英文书名斜体，日文书名自动添加『』
- 每本书之间自动插入分割线
- 编者/译者信息 12px 黑色
- 封面图居中，188px 宽带阴影
- 封面 URL 可留空

### 13. 编辑/审校信息

```
[staff:威武萌|编辑]

[staff:冬生、都市累人|审校]
```

- 右对齐，灰色 12px
- 名字和职位之间自动显示为 `丨`
- 连续的 staff 条目之间的空行会被自动移除

### 14. 关注引导区

```
---[follow]

东亚视角 全球视野

寻找东亚论述的"虫洞"与"黑洞"

点击下图关注"谓无名"

---[/follow]
```

- 居中显示，主题蓝色 14px 加粗
- 开头自动与上方内容空三行
- 空行会被忽略

### 15. 公众号关注卡片

```
<mp-common-profile data-pluginname="mpprofile" data-nickname="谓无名" data-alias="grandviewea" data-from="0" data-headimg="图片URL" data-signature="公众号简介" data-id="Mzg3MDc0MjkzNQ==" data-is_biz_ban="0"></mp-common-profile>
```

- 直接粘贴微信原生 HTML，编辑器会原样保留
- 预览区显示虚线占位框，粘贴到公众号编辑器后正常渲染

---

## 完整示例

以下是一个完整的文章排版示例：

```markdown
>>> 这是一段编者按内容，介绍文章的背景和主旨。

编者按的第二段内容。
<<<

[book:https://example.com/cover.jpg|书名|作者|出版社|2025]

## 第一个小标题

---[dot]

正文段落内容，支持脚注^①，也支持外文注释（Cool Japan）。

> 引用块内容，灰色文字带左侧竖线。

## 第二个小标题

---[dot]

另一段正文内容。

---[toc]

序 引言 / 001

**第一章 标题 / 029**

---[/toc]

---[notes]

^[① 脚注内容说明。]

^[② 另一条脚注。]

---[/notes]

---[note]

- ・ 本次推送内容为节选。
- ・ 感谢出版社授权转载。

---[/note]

---[bio-title:作者简介]

[bio-img:https://example.com/photo.jpg]

[bio:作者简介文字。]

---[/bio]

---[reading-title:延伸阅读]

[reading-book:书名|编者|译者|https://example.com/cover.jpg]

---[/reading]

[staff:名字|编辑]

[staff:名字|审校]

---[follow]

关注公众号

点击下方关注

---[/follow]

<mp-common-profile data-pluginname="mpprofile" data-nickname="公众号名" ...></mp-common-profile>
```

---

## 自定义语法速查表

| 语法 | 说明 |
|------|------|
| `>>> ... <<<` | 编者按框 |
| `-- 作者` | 引文署名（引用块内，右对齐） |
| `[style:lh=2.2\|ls=1px]` | 逐处样式覆盖（作用于紧随其后的那一块） |
| `<u>文字</u>` | 下划线 |
| `[book:封面\|书名\|作者\|出版社\|年份]` | 中文书籍信息 |
| `[enbook:封面\|Title\|Author\|Publisher\|Year]` | 英文书籍信息 |
| `[jpbook:封面\|書名\|著者\|出版社\|年]` | 日语书籍信息 |
| `![book](url)` | 书籍封面图 |
| `[universal:URL\|行1\|行2\|...]` | 通用图片标识 |
| `### 标题 / 作者` | 章节大标题 |
| `## 标题` | 小标题 |
| `---[dot]` | 点线分割线 |
| `^①` | 脚注编号 |
| `---[notes] ... ---[/notes]` | 注释区（可滚动） |
| `^[① 内容]` | 注释条目 |
| `---[note] ... ---[/note]` | 尾注区 |
| `---[toc] ... ---[/toc]` | 目录区（可滚动） |
| `---[bio-title:标题] ... ---[/bio]` | 作者简介区 |
| `[bio-img:url]` | 简介区照片 |
| `[bio:文本]` | 简介区文字 |
| `---[reading-title:标题] ... ---[/reading]` | 延伸阅读区 |
| `[reading-book:书名\|编者\|译者\|URL]` | 延伸阅读中文书 |
| `[reading-enbook:Title\|Editor\|Pub\|URL]` | 延伸阅读英文书 |
| `[reading-jpbook:書名\|編者\|出版社\|URL]` | 延伸阅读日文书 |
| `[staff:名字\|职位]` | 编辑/审校信息 |
| `---[follow] ... ---[/follow]` | 关注引导区 |
| `<mp-common-profile ...>` | 公众号关注卡片 |
